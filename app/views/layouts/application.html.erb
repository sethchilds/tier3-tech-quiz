<!DOCTYPE html>
<html>
  <head>
    <title>Tier 3 Quiz</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application" %>
  </head>

  <body>
    <header>
    <h1><%= link_to "Tier 3 Quiz", root_path %></h1>
    <% flash.each do |type, msg| %>
      <h3>
        <%= msg %>
      </h3>
    <% end %>
    </header>

    <%= yield %>

    <footer>
      <%= link_to("Home", root_path) if logged_in? %>
      <%= link_to("Login", login_path) if !logged_in? %>
      <%= link_to("Log out", :logout) if logged_in? %>
    </footer>
  </body>
</html>
