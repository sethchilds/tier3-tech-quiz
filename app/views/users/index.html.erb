<p style="color: green"><%= notice %></p>

<h1>Users</h1>

<table id="users">
  <% @users.each do |user| %>
    <%= render user %>
    <td>
      <%= link_to "Show this user", user %>
    </td>
    </tr>
  <% end %>
</table>

<%= link_to "New user", new_user_path %>
